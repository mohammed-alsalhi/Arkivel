# Tests

Before submission, record:

- `npm run marketplace:validate-pack -- path/to/manifest.json`
- Import preview result from `/admin/marketplace`
- Slot coverage against `src/lib/component-slots.ts`
- Keyboard/focus review for interactive controls
- Contrast and responsive screenshots for every declared slot
